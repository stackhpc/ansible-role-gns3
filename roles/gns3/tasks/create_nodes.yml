# create nodes specified in config file
---
- name: Create GNS3 Nodes
  create_nodes:
    project_name: "{{ gns3_project_name }}"
    node_type: "{{ gns3_node_type }}"
    cloud_ports_mapping: "{{ gns3_cloud_ports_mapping }}"

# meeting notes:

# list of nodes, links between
# switch node family type -> this will affect what bundles are downloaded

